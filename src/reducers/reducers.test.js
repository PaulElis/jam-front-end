import {
  query,
  albums,
  artists,
  favorites,
  new_artists,
} from './reducers'

import * as reducers from './reducers'

// describe('searchQuery', () => {
//   it('should return initial state', () => {
//     expect(reducers.RUN_SEARCH(undefined, {})).toEqual(query: '')
//   })
// })

it('dummy test', () => {
  expect(2+2).toBe(4);
})
